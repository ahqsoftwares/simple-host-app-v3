import 'package:flutter/material.dart';

class SettingsWidget extends StatelessWidget {
  const SettingsWidget({Key? key, required this.child, this.width = 5})
      : super(key: key);

  final Widget child;
  final int width;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
